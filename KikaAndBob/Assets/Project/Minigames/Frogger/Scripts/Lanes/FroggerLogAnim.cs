using UnityEngine;
using System.Collections;

public class FroggerLogAnim : MonoBehaviour {

	public Vector2 floatDistance = new Vector2(0, 1);
	protected iTweener floatAnim = null;


	void Start () {
		//gameObject.MoveTo(floatDistance).IsLocal(true).EaseType(iTween.EaseType.easeInOutQuad).lo
	}

}
